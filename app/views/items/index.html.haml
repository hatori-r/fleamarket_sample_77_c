%p#notice= notice
%h1 Items
%table
  %thead
    %tr
      %th Name
      %th Introduce
      %th Status
      %th Shipping fee
      %th Shipping area
      %th Date
      %th Price introduce
      %th User
      %th Category
      %th Brand
      %th{:colspan => "3"}
  %tbody
    - @items.each do |item|
      %tr
        %td= item.name
        %td= item.introduce
        %td= item.status
        %td= item.shipping_fee
        %td= item.shipping_area
        %td= item.date
        %td= item.price_introduce
        %td= item.user
        %td= item.category
        %td= item.brand
        %td= link_to 'Show', item
        %td= link_to 'Edit', edit_item_path(item)
        %td= link_to 'Destroy', item, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Item', new_item_path
