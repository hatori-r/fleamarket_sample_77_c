.background
  .my-page

    = render partial: "credit_header"
    .show
      .show__frame
        .show__frame__username
        .show__frame__title
          登録クレジットカード情報の削除一覧
        .credit_card
          .credit_card__frame
            .credit_card_info
              .credit_card_info__brand
              .credit_card_info__numbers
                .number
                  = "**** **** **** " + @customer_card.last4
                  
                  .expiration_date__title
                    有効期限
                  .expiration_date__info
                    = @exp_month + " / " + @exp_year
            .destroy__function
              .destroy__function__link
                -# = link_to "クレジットカード削除", credit_path(current_user.credit.id) , method: :delete do
                  削除する
                = link_to '戻る', users_show_path
                
      = render partial: "credit_footer"